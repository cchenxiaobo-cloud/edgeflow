apiVersion: v2
name: edgeflow
description: EdgeFlow 边缘计算平台 —— 云端 CloudCore 的部署 Chart（镜像构建与 Chart 完整化，WBS 8.5）
type: application

# Chart 版本号：Chart 本身每次变更递增
version: 0.1.0

# 应用版本号：对应被部署应用的版本（与 Makefile VERSION 保持一致）
appVersion: "v0.1.0"

keywords:
  - edgeflow
  - edge-computing
  - kubeedge
