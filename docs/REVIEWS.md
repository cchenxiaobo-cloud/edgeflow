# EdgeFlow 评审记录（WBS 9.2-9.5 文档与示例）

> - 评审对象：M5 前置文档（API-SPEC / DEPLOYMENT / HANDOFF / 示例教程）与配套示例（examples/demo.sh）。
> - 评审视角：本 Agent（文档与示例工程师）自评 + 对照 WBS 完成标准与项目实际代码逐项核对。
> - 归档状态：✅ **v0.1.0 定稿**（2026-08-14）。文档正文均已标注定稿版本。

---

## 1. 评审范围与方法

| 项 | 内容 |
|----|------|
| 评审对象 | `docs/API-SPEC.md`（9.2）、`docs/DEPLOYMENT.md`（9.3）、`docs/HANDOFF.md`（9.4）、`examples/README.md` + `examples/demo.sh`（9.5） |
| 评审方法 | ① 文档内容与代码逐项核对（路由注册/请求校验/响应结构/环境变量名）；② 示例在干净环境实际执行验证（两次完整 DEMO PASS + 清理残留检查）；③ 与 WBS 9 完成标准（新用户 15 分钟部署、CRD 字段文档齐全、温度传感器 Demo 跑通）对照 |
| 评审时间 | 2026-08-14 19:00-19:30 (Asia/Shanghai) |

---

## 2. 9.2 API-SPEC.md 评审

### 2.1 覆盖度核对（与代码逐项比对）

| 检查项 | 结果 |
|--------|------|
| `/healthz` | ✅ 已收录（§2，含请求/响应示例） |
| `GET /api/v1/nodes` / `{nodeID}` | ✅ 已收录（§3.1/3.2，含 NodeInfo 字段表） |
| `GET /api/v1/edgenodes` / `{nodeID}` | ✅ 已收录（§3.3/3.4，CRD 视角 + K8s List 风格说明） |
| `GET /api/v1/pods` / `nodes/{nodeID}/pods` | ✅ 已收录（§4，含 phase 取值与 404/空数组语义） |
| `GET /api/v1/devices` / `nodes/{nodeID}/devices` | ✅ 已收录（§5，含 properties/desired 字段说明） |
| `POST .../podsync` | ✅ 已收录（§6.1，含五态响应与边缘行为） |
| `POST .../config-sync` | ✅ 已收录（§6.2，含 kind 白名单与 Secret 明文限制） |
| `POST .../device-command` | ✅ 已收录（§5.3，含请求体字段表与错误语义） |
| 错误码表 | ✅ 已收录（§1.2，400/404/500/502/504 + 语义区分说明） |
| CRD 类型（M0-2 内容） | ✅ 保留归档（第二部分，EdgeNode/DeviceModel/Device 全字段） |
| 已知缺口 | ✅ §7/§8（K8s 接入迁移点、内存态限制、Secret 明文等） |

### 2.2 发现的问题与处理

| # | 问题 | 处理 |
|---|------|------|
| R9.2-1 | 原文档标题与内容仅覆盖 CRD（M0-2 视角），未包含任何 REST 端点 | 已重构为「REST API 参考 + CRD 类型定义」双部分结构，全部 13 个端点补齐 |
| R9.2-2 | 原文档无错误码表，504/502 语义易混 | 新增 §1.2 统一错误码表，并给出「404=没送达 / 502=被拒绝 / 504=可能送达」的判定指引 |
| R9.2-3 | 原示例 YAML 标注「尚未在真实集群验证」 | 保留该标注（CRD 尚未接入 K8s，属事实陈述）；REST 端点示例已全部经 demo.sh 实测 |

### 2.3 已知问题清单（归档保留）

- `P2`：`/api/v1/nodes` 与 `/api/v1/edgenodes` 双视图并存（设计取舍，见文档 §8）；
- `P2`：device-command 的 value 为 float64，string/boolean 属性暂无法下发；
- `P2`：config-sync 的 Secret value 明文存储（PROGRESS.md 待办）。

**归档结论：✅ v0.1.0 定稿**

---

## 3. 9.3 DEPLOYMENT.md 评审

### 3.1 覆盖度核对

| 检查项（WBS 9.3 / 任务要求） | 结果 |
|--------|------|
| 快速开始（5 分钟跑通） | ✅ 新增 §0：一键 Demo + 手动分步两种方式 |
| 镜像构建（8.5 内容保留） | ✅ §1（含构建参数/验证/多架构/推送） |
| Helm 部署 | ✅ §2（安装/验证/values 表/边缘接入） |
| mTLS 启用 | ✅ §4（含检查清单与 SAN 注入） |
| keadm 安装 | ✅ §3（init/join/reset + 引用 docs/KEADM.md） |
| 升级回滚指引 | ✅ §5（引用 docs/UPGRADE.md + Helm 现有路径 + 二进制升级注意） |
| 卸载/清理 | ✅ 新增 §6（Helm 卸载 / 单机清理 / keadm reset） |
| 故障排查 | ✅ §7（新增设备无数据/Pod 未创建/MQTT 数据面条目） |
| 验证清单 | ✅ §8（新增 demo.sh 两次 DEMO PASS 实测记录） |

### 3.2 发现的问题与处理

| # | 问题 | 处理 |
|---|------|------|
| R9.3-1 | 原文档缺少「快速开始」入口，新用户无法 5 分钟上手 | 新增 §0，一键脚本优先推荐 |
| R9.3-2 | 升级/回滚章节只覆盖 Helm，未覆盖二进制/边缘节点升级 | 新增 §5.2（备份 SQLite、先摘流再替换等注意事项） |
| R9.3-3 | 无卸载/清理指引 | 新增 §6（含按标签精确删除容器，避免误删其他容器） |

### 3.3 已知问题清单（归档保留）

- `P2`：keadm `upgrade`/`rollback` 命令由 M5 并行任务实现，本文档仅引用 `docs/UPGRADE.md`（合入后需复核 §5 引用有效性）；
- `P2`：镜像未推送公共/私有仓库，集群部署前需先推送；
- `P2`：mTLS 默认 SAN 仅本机，跨主机必须显式注入。

**归档结论：✅ v0.1.0 定稿**

---

## 4. 9.4 HANDOFF.md 评审

### 4.1 覆盖度核对

| 检查项（WBS 9.4 / 任务要求） | 结果 |
|--------|------|
| 代码结构 | ✅ §3（与当前仓库实际目录逐项核对，含 mappers/hack/examples 新增目录） |
| 开发流程 | ✅ §4（分支/开发/验证/提交/合入 + 多 Agent 提交纪律） |
| 测试规范 | ✅ §5（覆盖率门槛、CI、E2E 与专项冒烟） |
| 贡献指南 | ✅ §6（Issue/分支/commit message/审查/文档同步/安全底线） |
| 常见任务：加新 API | ✅ §7.1（handler→路由→测试→文档→验证 五步） |
| 常见任务：新 Mapper | ✅ §7.2（接口→MQTT→装配→测试→文档 五步） |
| 常见任务：新设备协议 | ✅ §7.3（含模拟器与文档要求） |
| 常用命令速查 | ✅ §8（与 HANDOFF 原版一致并核对全部环境变量名） |
| 交接检查单 | ✅ §11（更新为含 demo.sh DEMO PASS） |

### 4.2 发现的问题与处理

| # | 问题 | 处理 |
|---|------|------|
| R9.4-1 | 原文档「项目结构」停留在 M0 阶段（cloudcore healthz + edgecore 占位），与当前代码严重脱节 | 重写 §3 为当前完整结构（cloud/edge 各包职责、mappers、examples） |
| R9.4-2 | 原「如何开发下一个模块」指向 M4 收尾，已过期 | 更新为 ROADMAP 指引 + 标准流程 |
| R9.4-3 | 无测试规范与常见任务章节 | 新增 §5/§7 |
| R9.4-4 | 原版「已知限制」中「edgecore 占位」「设备管理未开始」已不成立 | 更新为当前真实限制（内存态、日志级别、资源上报等） |

### 4.3 已知问题清单（归档保留）

- `P2`：升级回滚专项合入后需在 §8 命令速查补 `keadm upgrade/rollback` 条目；
- `P2`：多 Agent 提交纪律（§4.1）为本次协作经验沉淀，后续可按实际执行情况调整。

**归档结论：✅ v0.1.0 定稿**

---

## 5. 9.5 示例与教程评审

### 5.1 示例设计评审（examples/demo.sh）

| 设计要求 | 实现 | 验证 |
|---------|------|------|
| 一键脚本（bash + set -euo pipefail） | ✅ `examples/demo.sh`（11 步，含逐步日志与彩色输出） | `bash -n` 语法检查通过 |
| 构建（make build） | ✅ 步骤 1（`EDGEFLOW_DEMO_SKIP_BUILD=1` 可跳过） | 实测构建成功 |
| 临时目录（mktemp） | ✅ SQLite/日志全部落在 mktemp 目录，结束自动删除 | 清理后无残留（实测 0 目录） |
| 启动 cloudcore + edgecore | ✅ 随机空闲端口（python3/lsof 双策略），healthz 就绪等待 | 实测通过 |
| 验证 healthz 200 | ✅ 步骤 2（curl -fsS + 超时等待） | 实测通过 |
| 节点注册（/api/v1/nodes） | ✅ 步骤 4（轮询 Ready） | 实测通过 |
| 下发 Pod（podsync nginx） | ✅ 步骤 5（断言 `"acked":true`） | 实测通过 |
| 验证容器（docker ps edgeflow-*） | ✅ 步骤 6（按命名规范精确匹配） | 实测通过 |
| 验证 Pod 状态（/api/v1/pods） | ✅ 步骤 7（断言 Running） | 实测通过 |
| 验证设备数据（/api/v1/devices） | ✅ 步骤 8（断言 sensor-01） | 实测通过 |
| 设备指令（device-command） | ✅ 步骤 9（断言 acked + desired 写入） | 实测通过 |
| MQTT 数据面可选段 | ✅ 步骤 10（检测到 mosquitto 才跑：遥测订阅 + 指令下发 + 日志断言） | 实测通过（本机有 broker） |
| 清理（进程/容器/临时目录） | ✅ 步骤 11（podsync delete 回收容器）+ trap EXIT 兜底 | 实测无残留 |
| 幂等（随机 node-id/端口冲突处理） | ✅ RUN_SUFFIX（时间戳+PID）+ 随机端口 + 唯一 Pod 名 | 连续两次运行均 DEMO PASS |
| 失败即退出并提示 | ✅ set -euo pipefail + die() + DEMO FAIL 输出日志路径 | 代码审查确认 |

### 5.2 干净环境实测记录（评审证据）

| 运行 | 时间 | 结果 | 备注 |
|------|------|------|------|
| 第 1 次 | 2026-08-14 19:11 | ✅ **DEMO PASS**（含 MQTT 数据面全段） | 随机端口 59468/59469/59470 |
| 第 2 次（幂等复验） | 2026-08-14 19:12 | ✅ **DEMO PASS**（含 MQTT 数据面全段） | 随机端口 59510+，与前次零冲突 |
| 残留检查（两次运行后） | 2026-08-14 19:12 | ✅ 0 容器 / 0 进程 / 0 临时目录 | 另清理了评审前手动测试遗留的 1 个 edgecore 进程（非脚本产物） |

### 5.3 教程覆盖度核对（examples/README.md）

| 检查项 | 结果 |
|--------|------|
| 架构（ASCII） | ✅ §2（云端/边缘/MQTT 三层 + 数据流标注） |
| 前置条件 | ✅ §3（含 mosquitto 可选与镜像缓存说明） |
| 步骤（构建→起 cloudcore→起 edgecore→下发 Pod→查看容器→设备数据流→MQTT 可选→清理） | ✅ §4 一键 + §5 分步（5.1-5.10 与任务要求顺序一致） |
| 预期输出 | ✅ §6（demo.sh 实测输出总览） |
| 排障 | ✅ §7（8 条常见现象） |
| 与 KubeEdge 温度传感器示例对标 | ✅ §8（对应关系表） |

### 5.4 已知问题清单（归档保留）

- `P2`：demo.sh 的 JSON 断言用 grep 精确匹配（Go 标准库 JSON 无空格输出，当前可靠）；后续若响应格式调整需同步更新；
- `P2`：nginx:1.25-alpine 首次运行需拉取镜像（受网络影响，脚本等待 60s）；
- `P2`：MOSQUITTO 段依赖 `mosquitto_sub/pub` 客户端命令；仅有 broker 无客户端时自动跳过并提示。

**归档结论：✅ v0.1.0 定稿**

---

## 6. 总体结论与归档

| 交付物 | 定稿状态 | 评审结论 |
|--------|---------|---------|
| docs/API-SPEC.md（9.2） | ✅ v0.1.0 定稿 | 13 端点全覆盖，错误码表齐全，示例实测 |
| docs/DEPLOYMENT.md（9.3） | ✅ v0.1.0 定稿 | 快速开始/Helm/mTLS/keadm/升级回滚/卸载清理齐全 |
| docs/HANDOFF.md（9.4） | ✅ v0.1.0 定稿 | 结构与代码同步，测试规范与常见任务齐备 |
| examples/README.md（9.5 教程） | ✅ v0.1.0 定稿 | 教程结构完整，预期输出来自实测 |
| examples/demo.sh（9.5 示例） | ✅ v0.1.0 定稿 | **两次干净环境 DEMO PASS，幂等，无残留** |

### 归档说明

- 本评审记录随 v0.1.0 一并归档；后续文档/示例变更需更新对应章节并重新记录评审。
- 遗留 P2 项（keadm upgrade/rollback 引用、镜像推送等）不阻塞定稿，随 M5 发布流程闭环。
- 并行任务边界已遵守：未改动 `cmd/keadm/upgrade.go`、`rollback.go`（其他 Agent 编写中）与 `docs/PROGRESS.md` 的 P2 段。
